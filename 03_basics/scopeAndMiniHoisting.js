// SCOPES in Java Script

const user = [1,2,3,4,5]
user.push(6) // This is allowed because we are not changing the reference of the user variable

console.log(user) // TypeError: Assignment to constant variable.